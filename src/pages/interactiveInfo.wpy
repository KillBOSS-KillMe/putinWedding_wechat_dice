<template>
  <view class="container">
    <image src="{{infoNode.img}}" />
    <view class="name">
      二狗子
      <text class="type" wx:if="{{infoNode.type == '1'}}">车队</text>
      <text class="type" wx:if="{{infoNode.type == '2'}}">主持</text>
      <text class="type" wx:if="{{infoNode.type == '3'}}">化妆</text>
    </view>
    <view class="str">已接了您的发帖单</view>
    <view class="info">
      <view class="name">
        <text class="type" wx:if="{{infoNode.type == '1'}}">车队</text>
        <text class="type" wx:if="{{infoNode.type == '2'}}">主持</text>
        <text class="type" wx:if="{{infoNode.type == '3'}}">化妆</text>
        <text class="name">{{infoNode.name}}</text>
      </view>
      <view>
        <text class="label">发布于</text>
        <text class="price">￥{{infoNode.price}}</text>
        <text class="user">{{infoNode.user}}/{{infoNode.address}}</text>
      </view>
      <view>发布于{{infoNode.time}}</view>
    </view>
    <view class="botton">
      <view class="botton1">联系客服</view>
      <view class="botton2">在线交流</view>
      <view class="botton2">在线交易</view>
    </view>
  </view>
</template>

<script>
  import wepy from 'wepy'
  export default class InteractiveInfo extends wepy.page {
    config = {
      navigationBarTitleText: '互动详情'
    }
    components = {}

    mixins = []

    data = {
      infoNode: {
        id: '1', 
        name: '宝马', 
        type: '1', 
        price: '1200', 
        user: '二狗子', 
        address: '西安市', 
        time: '2019.12.10',
        img: '../image/daw.jpg'
      },
    }

    computed = {
      
    }

    methods = {
    }

    events = {
      
    }

    onLoad() {
    }
  }
</script>
<style lang="less">
  .container{
    width: 750rpx;
    height: auto;
    border-top: 1rpx solid #EEEEEE;
  }
  image{
    width: 100rpx;
    height: 100rpx;
    border-radius: 100rpx;
    margin: 20rpx auto;
    margin-top: 40rpx;
  }
  .name{
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 32rpx;
    font-weight: 600;
    color: #1F1F1F;
    padding-bottom: 20rpx;
    .type{
      margin-left: 10rpx;
    }
  }
  .str{
    width: 750rpx;
    border-bottom: 1rpx solid #EEEEEE;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 28rpx;
    color: #1F1F1F;
    font-weight: 600;
    padding-bottom: 50rpx;
  }
  .type{
    color: #F42652;
    font-size: 20rpx;
    font-weight: 500;
    padding: 5rpx 10rpx;
    border: 1rpx solid #F42652;
    border-radius: 5rpx;
    margin-right: 10rpx;
  }
  
  .info{
    width: 690rpx;
    height: auto;
    border-bottom: 1rpx solid #EEEEEE;
    margin: 0 30rpx;
    padding: 20rpx 0;
    view{
      font-size: 24rpx;
      color: #494949;
      padding: 10rpx 0;
    }
    .name{
      justify-content: flex-start;
      padding-bottom: 10rpx;
      .type{
        margin-right: 10rpx;
      }
    }
  }
  .price{
    color: #F42652;
    font-size: 26rpx;
    font-weight: 600;
    margin-right: 10rpx;
  }
  .label{
    color: #494949;
    font-size: 26rpx;
  }
  .botton{
    width: 690rpx;
    padding: 30rpx;
    margin-top: 200rpx;
    display: flex;
    align-items: center;
    justify-content: space-around;
    view{
      font-size: 32rpx;
      font-weight: 600;
      padding: 20rpx;
      border: 1rpx solid;
      border-radius: 10rpx;
    }
    .botton1{
      color: #1F1F1F;
      background-color: #fff;
      border-color: #999999;
    }
    .botton2{
      color: #fff;
      background-color: #F42652;
      border-color: #F42652;
    }
  }
</style>